<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>wiki</title>
    <link rel="stylesheet" href="<%= Path.join(@root_path, "style.css") %>" />
  </head>
  <body class="px-6 py-16 mx-auto max-w-7xl lg:px-8">
    <%= EEx.eval_file("templates/_header.html.eex", assigns: [categories: [], title: @category, filename: "index.html"]) %>

    <ul class="mt-8">
      <%= for sub_category <- @sub_categories do %>
        <li><a href="<%= Path.join([Wiki.Config.output_directory(), sub_category, "index.html"]) %>"><%= Path.basename(sub_category) %></a></li>
      <% end %>

      <%= for page <- Enum.sort_by(@pages, &(&1.title)) do %>
        <li><a href="<%= Path.join(Wiki.Config.output_directory(), page.filename) %>"><%= page.title %></a></li>
      <% end %>
    </ul>

    <%= EEx.eval_file("templates/_footer.html.eex", assigns: []) %>
  </body>
</html>
